const _initialState = {
  data: {
    list: [],
    active: null,
  },
  isLoading: false,
  error: null,
};

const projectVersionReview = (state = _initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default projectVersionReview;