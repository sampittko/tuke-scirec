const _initialState = {
  data: {
    list: null,
    active: null,
  },
  isLoading: false,
  error: null
};

const projectVersion = (state = _initialState, action) => {
  return _initialState;
};

export default projectVersion;